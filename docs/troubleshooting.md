# Troubleshooting

Common checks:

1. `dam status` / `dam health`
2. Verify upstream auth headers
3. Confirm `server.*_upstream_url` settings
4. Validate config (`dam config validate`)
5. Check vault/key availability and file permissions

Detailed failure patterns and remediation steps belong here to keep README concise.
