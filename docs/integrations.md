# Integrations

Detailed client integration examples and config snippets for:

- HTTP proxy usage (`/v1/messages`, `/v1/chat/completions`, `/v1/responses`, `/codex/responses`)
- MCP wiring for Claude Code, Codex, OpenClaw, and compatible clients
- Language SDK examples (Python/TypeScript/curl)

> This file is intentionally separated from the README to keep first-run docs short.
