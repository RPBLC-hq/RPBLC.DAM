{
  "id": "claude-code",
  "name": "Claude Code",
  "summary": "Route Claude Code's normal Anthropic HTTPS traffic through DAM.",
  "provider": "anthropic",
  "traffic_app_ids": ["anthropic-api"],
  "connect_args": [
    "--anthropic",
    "--network-mode",
    "tun",
    "--trust-mode",
    "local_ca"
  ],
  "settings": [
    {
      "key": "HTTPS_PROXY",
      "value": "{{proxy_url}}",
      "description": "HTTPS proxy for explicit-proxy fallback"
    },
    {
      "key": "HTTP_PROXY",
      "value": "{{proxy_url}}",
      "description": "HTTP proxy for explicit-proxy fallback"
    }
  ],
  "commands": [
    {
      "label": "Start DAM for Claude Code",
      "command": [
        "dam",
        "connect",
        "--profile",
        "claude-code"
      ]
    },
    {
      "label": "Run Claude Code through explicit-proxy fallback",
      "command": [
        "env",
        "{{https_proxy_env}}={{proxy_url}}",
        "{{http_proxy_env}}={{proxy_url}}",
        "claude"
      ]
    }
  ],
  "notes": [
    "Claude Code keeps its normal Anthropic API endpoint; Network Extension capture is primary and explicit proxy remains a fallback for source builds and unsupported environments.",
    "`dam integrations apply claude-code` ensures a DAM-owned JSON profile file under the integration profile directory; it does not mutate Claude Code settings.",
    "Use the listed explicit-proxy fallback command only for source builds and unsupported environments.",
    "Claude Code keeps provider authentication; DAM only receives and forwards the request headers."
  ],
  "automation": "connect_preset"
}
