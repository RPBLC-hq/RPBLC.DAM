paths-ignore:
  # Test files contain intentional pattern-matching strings that look like
  # secrets — they are synthetic test data used to validate PII detection,
  # not real credentials.
  - "crates/dam-detect/src/**"
